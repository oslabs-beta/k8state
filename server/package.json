{
	"name": "server",
	"version": "1.0.0",
	"type": "module",
	"main": "server.js",
	"scripts": {
		"server": "concurrently \"tsc --watch\" \"nodemon server.js\"",
		"build": "tsc",
		"watch": "tsc --watch",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"description": "",
	"dependencies": {
		"concurrently": "^8.2.2",
		"vite": "^5.4.1",
		"ts-node": "^10.9.2",
		"typescript": "^5.5.4"
	},
	"devDependencies": {
		"nodemon": "^3.1.4"
	}
}
